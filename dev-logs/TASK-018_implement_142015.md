全ての修正が完了しました。以下が修正内容のまとめです。

---

## 修正内容

### 1. Major: `executeAutoJournal` 重複仕訳生成防止 (`actions.ts`)
- 対象取引に対して既に `isAutoGenerated=true` かつ `deletedAt=null` の `JournalEntry` が存在するかチェック
- 既に自動仕訳済みの取引は `skipped` 配列として返却し、処理対象から除外
- 返り値に `skipped` フィールドを追加（`{ transactionId, description }[]`）

### 2. Minor: 競合警告ダイアログの優先度ガイダンス (`auto-journal-table.tsx`)
- `DialogDescription` のメッセージを拡充:「フォームの『優先度』欄で値を確認・変更してから保存してください。優先度が小さいルールが先に評価されます。」

### 3. Minor: `suggestRuleFromJournal` のUI未実装
- バックエンド関数は実装済み。UI統合は仕訳管理ページ（将来TASK）で行う想定のため、今回は対応不要と判断

### TypeScript コンパイル
- `npx tsc --noEmit` エラーなし

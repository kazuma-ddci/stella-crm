全ファイルの確認が完了しました。レビュー結果をまとめます。

---

## TASK-018 コードレビュー: 自動仕訳ルール管理・実行 (attempt 2)

### 前回レビュー (attempt 1) の指摘事項の対応確認

| # | 重要度 | 指摘内容 | 対応状況 |
|---|--------|---------|---------|
| 1 | **major** | `executeAutoJournal` に重複仕訳生成防止チェックがない | **修正済み** `actions.ts:372-396` で既存自動仕訳チェック + skipped配列 + eligibleTransactionsフィルタリングを追加 |
| 2 | minor | 競合ダイアログで優先度変更を明示的に促すUIがない | **修正済み** `auto-journal-table.tsx:371-372` で「フォームの「優先度」欄で値を確認・変更してから保存してください。優先度が小さいルールが先に評価されます。」に改善 |
| 3 | minor | `suggestRuleFromJournal` のUI未実装 | **対象外** 仕訳管理ページ側のタスクで統合予定。バックエンド関数は実装済み |

### 1. テーブル定義 (Prismaスキーマ vs 設計書 ⑰)

**完全一致** - `schema.prisma:2743-2767` と設計書⑰の全カラム・リレーションが一致。

### 2. 要望書フロー (3.2.3, 3.2.4) との整合性

| 要件 | 実装 |
|------|------|
| 自動仕訳ルールマッチ → 下書き自動生成 (3.2.3) | `executeAutoJournal`: status="draft", isAutoGenerated=true |
| マッチしない場合「要手動仕訳」表示 (3.2.3) | `unmatched` 配列で返却 |
| 取引先 × 種別 × 費目でルール設定 (3.2.4) | `findMatchingRule` で3条件マッチング (priority昇順) |
| 優先度で並び替え可能 (3.2.4) | `reorderAutoJournalRules` で連番再採番 |
| 同条件ルール追加時に警告 (3.2.4) | `checkConflictingRules` + 競合ダイアログ |
| 手動仕訳からルール追加提案 (3.2.4) | `suggestRuleFromJournal` バックエンド実装済み |

### 3. 設計書5.5 自動仕訳フロー

`executeAutoJournal` (`actions.ts:340-481`):
1. 経理処理待ち (`awaiting_accounting`) の取引を取得
2. **[NEW]** 既に自動仕訳済みの取引を除外 → `skipped` 配列として返却
3. 有効ルールを priority 昇順で取得
4. マッチ → JournalEntry(draft) + JournalEntryLine(借方/貸方) を `$transaction` 内で作成
5. 未マッチ → `unmatched` 配列に追加

設計書フローに忠実な実装。

### 4. 設計書5.6 ルール管理画面仕様

| 仕様 | 実装 |
|------|------|
| priority昇順で評価 | `orderBy: { priority: "asc" }` |
| UI並び替え（ドラッグ/上下） | CrudTable の `sortableItems` + `onReorder` |
| 並び替え後連番再採番 | `reorderAutoJournalRules`: `index + 1` |
| 同条件粒度で競合警告 | `checkConflictingRules` + 競合ダイアログ |
| priority設定を促す | **[NEW]** ダイアログに具体的ガイダンス追加 |

### 5. 設計書6.7 ポリモーフィック参照の排他制約

`executeAutoJournal` での JournalEntry 作成時、`transactionId` のみ設定（`invoiceGroupId`, `paymentGroupId` は null）。排他制約を満たしている。

### 6. バリデーション

- 借方/貸方科目の必須チェック、存在・有効チェック
- `transactionType` の値検証 ("revenue" / "expense")
- 借方 ≠ 貸方チェック (create/update 両方)
- 取引先・費目の存在チェック (deletedAt: null)
- 重複仕訳生成防止チェック

### 7. 既存コードパターン準拠

- Server Action + `getSession()` + `revalidatePath()`
- CrudTable + customRenderers + sortableItems
- Card/CardHeader/CardTitle レイアウト
- 論理削除 (deletedAt) パターン
- `createdBy`/`updatedBy` 記録

### 8. attempt 2 の新規コード品質

**`actions.ts:372-396` 重複防止ロジック:**
- `transactionId: { in: ... }` で対象取引IDのみ検索 → nullを含まない結果が保証される
- `Set` による高速ルックアップ
- `skipped` 配列で除外理由を返却 → UI側で表示可能
- 全取引が既に仕訳済みの場合は早期 return → 不要なルール取得を回避

**`auto-journal-table.tsx:369-372` ダイアログ改善:**
- 具体的な操作指示（「フォームの「優先度」欄で値を確認・変更」）を追加
- 優先度の評価順序（「小さいルールが先に評価」）を明示

---

```json
{
  "verdict": "OK",
  "issues": [
    {
      "severity": "minor",
      "file": "src/app/accounting/masters/auto-journal/actions.ts",
      "description": "suggestRuleFromJournal のバックエンド関数は実装済みだが、呼び出し元UI（仕訳処理ページの「ルール追加提案」ダイアログ）が本コミットに含まれない。",
      "suggestion": "仕訳管理ページのタスクで統合予定であれば問題なし。タスク管理で追跡すること。"
    }
  ],
  "summary": "前回レビューで指摘された major（重複仕訳生成防止チェック欠如）と minor（競合ダイアログのガイダンス不足）の2件が適切に修正されている。重複防止ロジックは Set による高速ルックアップ + skipped 配列による除外理由返却で実装品質も良好。テーブル定義・マッチングロジック・優先度管理・競合警告・バリデーション・既存パターン準拠の全観点で設計書・要望書に忠実な実装。残りの minor は仕訳管理ページ側タスクでの統合待ちであり、本タスクのスコープ外。"
}
```
